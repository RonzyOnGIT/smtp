## Simplified SMTP client and server

First, run `python3 smtp_server.py`, then run `python3 smtp_client.py`

To get ur IP address to connect to ur server:

 - Linux - `ip a`
  
 - Windows - `ipconfig`

 - Unix - `ifconfig`
